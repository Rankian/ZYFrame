<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/loading_view"
    android:layout_width="100dp"
    android:layout_height="100dp"
    android:layout_gravity="center"
    android:gravity="center"
    android:padding="@dimen/ZYFrame_spacing"
    android:background="@drawable/loading_dialog_bg"
    android:orientation="vertical">


    <ProgressBar
        android:id="@+id/loading_progressbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:indeterminateDrawable="@drawable/progress_bar_indeterminate"/>

    <TextView
        android:id="@+id/loading_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/ZYFrame_spacing"
        android:text="@string/loading_dialog_message"
        android:textColor="@color/ZYFrame_loading_dialog_message"
        android:textSize="@dimen/ZYFrame_text_size_small" />

</LinearLayout>
